angular
    .module('workFlo.filteredRequestPage') // No array, as we are "extending" the module
    .controller('FilteredRequestPageCtrl', FilteredRequestPageCtrl); // Define our controller (Notice the naming convention - uppercase first letter, Ctrl suffix)

function FilteredRequestPageCtrl(library) {
    var vm = this; // vm stands for View Model - anything on "the vm" is exposed to the view


        
    
}
