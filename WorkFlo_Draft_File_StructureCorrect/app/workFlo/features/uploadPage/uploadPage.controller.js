angular
    .module('workFlo.uploadPage') // No array, as we are "extending" the module
    .controller('UploadPageCtrl', UploadPageCtrl); // Define our controller (Notice the naming convention - uppercase first letter, Ctrl suffix)

function UploadPageCtrl(library) {
    var vm = this; // vm stands for View Model - anything on "the vm" is exposed to the view


        
    
}
