angular
    .module('workFlo.clickedImage') // No array, as we are "extending" the module
    .controller('ClickedImageCtrl', ClickedImageCtrl); // Define our controller (Notice the naming convention - uppercase first letter, Ctrl suffix)

function ClickedImageCtrl() {
    var vm = this; // vm stands for View Model - anything on "the vm" is exposed to the view


        
    
}
