angular
    .module('workFlo.randomSearchPage') // No array, as we are "extending" the module
    .controller('RandomSearchPageCtrl', RandomSearchPageCtrl); // Define our controller (Notice the naming convention - uppercase first letter, Ctrl suffix)

function RandomSearchPageCtrl(library) {
    var vm = this; // vm stands for View Model - anything on "the vm" is exposed to the view


        
    
}
