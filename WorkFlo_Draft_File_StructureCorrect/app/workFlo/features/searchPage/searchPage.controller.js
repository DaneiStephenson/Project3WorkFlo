angular
    .module('workFlo.searchPage') // No array, as we are "extending" the module
    .controller('SearchPageCtrl', SearchPageCtrl); // Define our controller (Notice the naming convention - uppercase first letter, Ctrl suffix)

function SearchPageCtrl() {
    var vm = this; // vm stands for View Model - anything on "the vm" is exposed to the view


        
    
}
