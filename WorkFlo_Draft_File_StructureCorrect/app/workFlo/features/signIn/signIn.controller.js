angular
    .module('workFlo.signIn') // No array, as we are "extending" the module
    .controller('SignInCtrl', SignInCtrl); // Define our controller (Notice the naming convention - uppercase first letter, Ctrl suffix)

function SignInCtrl() {
    var vm = this; // vm stands for View Model - anything on "the vm" is exposed to the view


        
    
}
