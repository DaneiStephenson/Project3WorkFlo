angular
    .module('workFlo.signUp') // No array, as we are "extending" the module
    .controller('SignUpCtrl', SignUpCtrl); // Define our controller (Notice the naming convention - uppercase first letter, Ctrl suffix)

function SignUpCtrl() {
    var vm = this; // vm stands for View Model - anything on "the vm" is exposed to the view


        
    
}
