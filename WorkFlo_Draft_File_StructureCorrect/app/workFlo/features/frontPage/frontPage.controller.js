angular
    .module('workFlo.frontPage') // No array, as we are "extending" the module
    .controller('FrontPageCtrl', FrontPageCtrl); // Define our controller (Notice the naming convention - uppercase first letter, Ctrl suffix)

function FrontPageCtrl() {
    var vm = this; // vm stands for View Model - anything on "the vm" is exposed to the view


        
    
}
