angular
    .module('workFlo.loginHomePage') // No array, as we are "extending" the module
    .controller('LoginHomePageCtrl', LoginHomePageCtrl); // Define our controller (Notice the naming convention - uppercase first letter, Ctrl suffix)

function LoginHomePageCtrl() {
    var vm = this; // vm stands for View Model - anything on "the vm" is exposed to the view


        
    
}
